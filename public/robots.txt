# The whole site is open to crawlers, including AI answer engines.
# The FAQ and capability pages are written to be quotable, and being
# cited in an AI answer is the point. Named per-bot rules are omitted
# deliberately: the wildcard below already permits every crawler, and
# the AI opt-outs are all opt-out rather than opt-in.

User-agent: *
Allow: /

Sitemap: https://www.propelsaga.com/sitemap.xml
