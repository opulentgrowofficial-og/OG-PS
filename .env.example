# Where the enquiry form POSTs its JSON payload.
# Leave unset in development: the form will validate and then surface a
# send failure, which is the intended behaviour until a real endpoint exists.
VITE_ENQUIRY_ENDPOINT=
